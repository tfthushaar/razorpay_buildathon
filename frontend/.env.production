# The deployed backend has no GPU and no local model, so ollama cannot work there. The local default
# stays ollama (see RunControls.tsx); the deployed default is the one that actually runs.
VITE_DEFAULT_PROVIDER=mock
