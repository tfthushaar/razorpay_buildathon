# Copy to .env and fill in. Never commit the real .env (already gitignored).

# "mock" (default, zero-cost, deterministic) or "groq"
LLM_PROVIDER=mock

# Free tier at https://console.groq.com — only needed when LLM_PROVIDER=groq
GROQ_API_KEY=
